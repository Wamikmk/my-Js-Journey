// ------------ Immediately Invoked Function Expression-----------

// Wrap the function inside ()
// To invoke use ()
// While writing two IIFIs make sure to put ; after the first IIFE


((names) => (console.log(`Hello ${names}`))) ("John") ;


(function hello(names) {
    console.log(`Hi ${names}`)
}) ("Jenny")




